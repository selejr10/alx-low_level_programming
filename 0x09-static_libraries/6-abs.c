#include "main.h"
/**
 * _abs - A function that computes the absolute value of an integer
 * @n: the integer to be checked
 * Return: the exact value of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
